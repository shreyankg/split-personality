{
  "name": "split-personality",
  "version": "1.0.0",
  "description": "Split Personality - A household chore management app that assigns monetary values to chores for equitable distribution",
  "main": "backend/dist/index.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run install:all && npm run db:generate && npm run build:frontend && npm run build:backend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "db:generate": "cd backend && npm run db:generate",
    "start": "node simple-server.js",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "db:setup": "cd backend && npm run db:generate && npm run db:push",
    "db:migrate": "cd backend && npm run db:migrate"
  },
  "keywords": ["split-personality", "chore", "equity", "household", "management"],
  "author": "Split Personality Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}